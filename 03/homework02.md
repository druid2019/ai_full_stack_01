1. 使用 DeepSeek 在 Python/Java/golang 等任意一门主流编程语言上，实践本节
   课程提示词技巧，生成一个最基本的用户登录模块，以及单元测试代码，



```
生成一个最基本的用户登录模块，并给出对应的单元测试代码。
具体要求：
包含注册，登陆，忘记密码等功能
注册需要填写邮箱或手机号码,用户名和密码，密码需要二次校验一致方可通过，
用户名可以与注册用的邮箱或手机号一致，也可以另外填写
用户名需不重复及是否存在的校验，且以数字、字母或其他字符命名
密码需要包含字母、数字和其他字符的组合，长度不小于6。
登陆需要用户名和密码
还需添加忘记密码，通过邮箱或手机号码找回

邮箱可编写一个名为`is_valid_emai'的函数，用于精确验证一个给定的字符串是否为有效的电子邮件地址。
函数`is_valid_email(email_string)要求:
1.接受-个字符串参数`email_string`。
2.如果 `email string`不是字符串类型，应抛出`TypeError'异常，并附带清晰的错误信息。
3、使用一个健壮的、能覆盖常见有效邮箱格式(包括带"+'号的别名、多级域名、国际化域名中的非ASCI字符暂不考虑)的正
则表达式。请在代码中注释该正则表达式的来源或设计思路。
4.函数返回`True`表示邮箱格式有效，`False'表示无效。
然后，请使用 Python 的`mailtest`模块为`is_valid_emai'函数编写一个测试类`TestlsValid'.
测试类应包含以下测试方法:
*'test_valid_emails:测试至少5个不同类型的有效邮箱地址(例如:简单邮箱,带点的用户名,带连字符的域名,带’+'的邮
箱..co.uk等多级域名)，
test_invalid_emails_format:测试至少5个格式明显错误的邮箱地址(例如:缺少@,缺少域名,@在开头,域名含非法字符
连续的点等)。
test_invalid_input_type:测试当输入不是字符串时(例如整数、列表、None)是否如期抛出`TypeError'。
test_empty_string:测试输入为空字符串时的情况。
义
test_emails_with_spaces:测试邮箱地址前后或中间包含空格的情况(通常应视为无效)
请确保测试用例覆盖全面，并且输出完整的、可直接运行的 Python 代码文件内容，包含函数定义和`unittest' 测试代码。

手机号验证类似，编写一个名为`is_valid_phone'的函数，用于精确验证一个给定的字符串是否为有效的手机号码。
函数`is_valid_phone(phone_string)要求:
1.接受-个字符串参数`phone_string`。
2.如果 `phone_string`不是字符串类型，应抛出`TypeError'异常，并附带清晰的错误信息。
3、手机号为11位，只接受中国大陆的。
4.函数返回`True`表示手机号格式有效，`False'表示无效。
然后，在测试类`TestlsValid'中添加一个方法校验phone.

密码和以上类似，编写一个名为`is_valid_password'的函数，用于精确验证一个给定的字符串是否为有效的密码。
需要包含字母、数字和其他字符的组合，长度不小于6。
然后，在测试类`TestlsValid'中添加一个方法校验password.
```

